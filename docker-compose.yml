version: '3.8'

services:
  opencode:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: opencode_cli
    stdin_open: true
    tty: true
    user: "1000:1000"
    volumes:
      - /home/kauezao:/workspace
    working_dir: /workspace
